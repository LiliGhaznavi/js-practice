// Exercise 2: Skipping Elements
// Extract the first and third elements from the `colors` array (skip the second one).

const colors = ["red", "blue", "green", "yellow"];

// Your code here
const [first, , third] = colors;
console.log(first, third);
